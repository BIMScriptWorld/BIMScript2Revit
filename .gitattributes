# Normalise to LF in the repository AND in the working tree.
# ifc/BIMScript_Properties.txt is compared byte-for-byte against the text
# generated by bimscript_core/ifc.py, so a CRLF checkout on Windows would
# fail the test suite on a fresh clone.
* text=auto eol=lf

*.png binary
